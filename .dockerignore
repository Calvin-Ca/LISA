# Legacy Docker builder reads only the context-root ignore file.
# Keep the production image context allowlisted to runtime source trees.
**
!production/
!production/**
!model/
!model/**
!utils/
!utils/**
!.dockerignore

# Never send local runtime configuration or secrets to the daemon.
**/.env
**/.env.*
